<?php    $title = 'Peliculas';?>
<?php require 'templates/layout/header.phtml'; ?>

<div>
    <div class="categorias-container">
        <!-- categorias // input categorias-->
        <div class="listado-categorias">
            <span>Géneros:</span>
            <ul class="lista-generos-btn">
            <?php foreach($generos as $genero): ?>
                <li>
                    <span class="nombre-genero"><a href = "<?= htmlentities(BASE_URL) ?>peliculas/<?= htmlspecialchars($genero->nombre)?>"><?= htmlspecialchars($genero->nombre)?></a></span>
                </li>
            <?php endforeach ?>

            </ul>
        </div>
        <select id="input-genero" name="input-genero">
            <option value="" selected>Género:</option>
            <option value="Comedia">Comedia</option>
            <option value="Accion">Accion</option>
            <option value="Terror">Terror</option>
            <option value="Romance">Romance</option>
        </select>
    </div>
    <div class="peliculas-container"><!-- peliculas -->
    <?php foreach($peliculas as $pelicula): ?>
        <div class="poster"><!-- pelicula -->
                <a href="pelicula/<?= htmlspecialchars($pelicula->id)?>">
                    <img src="<?= htmlspecialchars(BASE_URL . '/frontend/images/' .$pelicula->path_img) ?>" alt="" srcset="">
                </a>
                <?php if ($this->res->user && $this->res->user->role == 'admin'): ?>
                    <a href="eliminarPelicula/<?= htmlspecialchars($pelicula->id)?>"><button id="delete-button">X</button></a>
                <?php endif; ?>
        </div>
    <?php endforeach ?>
    </div>
</div>
<script src="frontend/js/home.js"></script>
<?php require_once 'templates/layout/footer.phtml'; ?>